<p style="color: green"><%= notice %></p>

<%= render @contest_instance %>

<div>
  <%= link_to "Edit", edit_contest_instance_path(@contest_instance) %> |
  <%= link_to "Back", contest_instances_path %>

  <%= button_to "Destroy", method: :delete, data: { controller: 'confirm', confirm_message_value: 'Are you sure you want to delete this?' } %>
</div>
