<p style="color: green"><%= notice %></p>

<h1>Campuses</h1>

<div id="campuses">
  <% @campuses.each do |campus| %>
    <%= render campus %>
    <p>
      <%= link_to "Show", campus %>
    </p>
  <% end %>
</div>

<%= link_to "New", new_campus_path %>
