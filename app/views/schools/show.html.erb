<p style="color: green"><%= notice %></p>

<%= render @school %>

<div>
  <%= link_to "Edit", edit_school_path(@school) %> |
  <%= link_to "Back", schools_path %>

  <%= button_to "Destroy", @school, method: :delete, data: { controller: 'confirm', confirm_message_value: 'Are you sure you want to delete this?' } %>
</div>
