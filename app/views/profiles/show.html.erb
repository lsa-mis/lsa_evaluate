<div class="mt-4 d-flex justify-content-between" >
  <div class="col-10">
    <%= render @profile %>
  </div>
  <div>
    <%= button_to "Update Profile", edit_profile_path(@profile), class: "btn btn-warning", method: :get %>
  </div>
</div>

<div>
  <%= link_to "Back", applicant_dashboard_path %>

  <%#= button_to "Destroy", @profile, method: :delete, data: { controller: 'confirm', confirm_message_value: 'Are you sure you want to delete this?' } %>
</div>
