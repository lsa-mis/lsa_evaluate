<div data-controller="scroll" data-scroll-scroll-to-top-value="<%= params[:scroll_to_top].present? %>">
  <div class="mt-4 d-flex justify-content-between" >
    <div class="col-10">
      <%= render @profile %>
    </div>
    <div>
      <div>
        <%= button_to "Update Profile", edit_profile_path(@profile), class: "btn btn-warning", method: :get %>
      </div>
      <div>
        <%= link_to "Back to dashboard", applicant_dashboard_path %>
      </div>
    </div>
  </div>
</div>
