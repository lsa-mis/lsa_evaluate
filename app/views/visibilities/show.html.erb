<p style="color: green"><%= notice %></p>

<%= render @visibility %>

<div>
  <%= link_to "Edit", edit_visibility_path(@visibility) %> |
  <%= link_to "Back", visibilities_path %>

  <%= button_to "Destroy", @visibility, method: :delete %>
</div>
