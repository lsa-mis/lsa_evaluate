<div class="container d-flex justify-content-between mt-4">
  <h1>Manage Submissions</h1> 
  <div>
    <%= link_to "Create New Contest", new_container_path, {class: 'btn btn-sm btn-primary'} %>
  </div>
</div>

<div class="container-fluid d-flex flex-column justify-content-between mt-4">
  <% @containers.each do |container| %>
    <div class="row mt-2">
      <div class="">
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0"><%= container.name %></h5>
              <small class="text-muted">
                <%= container.department.display_name %>
                <br> 
                <% admins = container.assignments.container_administrators %>
                <% if admins.any? %>
                  <%= pluralize(admins.count, admins.first.role.kind) %>:
                  <%= admins.map { |assignment| assignment.user.display_name_or_email }.join(', ') %>
                <% else %>
                  No Administrators Assigned
                <% end %>
              </small>
            <%= link_to "View Contest Details", container_path(container), class: "link_to" %>
          </div>
          <div class="card-body">
            <%= link_to "Create a New Contest", new_container_contest_description_path(container), class: "btn btn-sm btn-secondary" %>
            <%= link_to "Create Multiple Instances", contest_descriptions_for_container_container_contest_descriptions_path(container), class: "btn btn-sm btn-secondary" %>
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Short Name</th>
                  <th>Eligibility Rules</th>
                  <td>Actions</td>
                </tr>
              </thead>
              <tbody>
                <% container.contest_descriptions.each do |description| %>
                    <tr>
                    <td><%= description.name %></td>
                    <td><%= description.short_name %></td>
                    <td><%= description.eligibility_rules %></td>
                    <td>
                      <%= link_to "View contest instances", container_contest_description_contest_instances_path(container, description), class: "link_to" %>
                      <% unless description.contest_instances.where(active: true).present? %>
                        <% if description.archived %>
                          | <%= link_to "Unarchive", unarchive_contest_description_container_contest_descriptions_path(container, description), data: { turbo_confirm: "Are you sure you want to unarchive this contest description?", turbo_method: :post } %>
                        <% else %>
                          | <%= link_to "Archive", archive_contest_description_container_contest_descriptions_path(container, description), data: { turbo_confirm: "Are you sure you want to archive this contest description?", turbo_method: :post } %>
                        <% end %>
                      <% end %>
                    </td>
                    </tr>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
