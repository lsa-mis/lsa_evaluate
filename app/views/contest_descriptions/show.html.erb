<%= render @contest_description %>

<div>
  <%= link_to "Edit", edit_container_contest_description_path([@container, @contest_description]) %> |
  <%= link_to "Return to Manage Collections", containers_path %>

  <%= button_to "Destroy", container_contest_description_path([@container, @contest_description]), method: :delete, data: { controller: 'confirm', confirm_message_value: 'Are you sure you want to delete this?' } %>
</div>
