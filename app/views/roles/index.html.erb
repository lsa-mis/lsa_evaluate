<p style="color: green"><%= notice %></p>

<h1>Roles</h1>

<div id="roles">
  <% @roles.each do |role| %>
    <%= render role %>
    <p>
      <%= link_to "Show", role %>
    </p>
  <% end %>
</div>

<%= link_to "New", new_role_path %>
