default: &default
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: <%= ENV.fetch("DATABASE_USERNAME") { "root" } %>
  password: <%= ENV.fetch("LOCAL_MYSQL_DATABASE_PASSWORD") { "" } %>
  port: 3306

test:
  <<: *default
  database: lsa_evaluate_test
